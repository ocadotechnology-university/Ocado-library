# Google OAuth2 (logowanie)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# PostgreSQL
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=dev_database

# JWT (min. 32 znaki w produkcji)
JWT_SECRET=ocado-library-dev-jwt-secret-change-in-production-min-32-chars

# Frontend URL (OAuth redirect)
FRONTEND_URL=http://localhost:5173

# Slack — powiadomienia DM (opcjonalne lokalnie)
SLACK_ENABLED=false
SLACK_BOT_TOKEN=
